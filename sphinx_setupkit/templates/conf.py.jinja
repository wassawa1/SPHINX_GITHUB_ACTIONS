# Configuration file for the Sphinx documentation builder.
#
# For the full list of built-in configuration values, see the documentation:
# https://www.sphinx-doc.org/en/master/usage/configuration.html

# === Additional Settings ----------------------------------------------------
# Adding the parent directory to the system path
# This allows Sphinx to find and import modules from the project directory
import os
import sys
sys.path.insert(0, os.path.abspath('../'))

# -- Project information -----------------------------------------------------
# https://www.sphinx-doc.org/en/master/usage/configuration.html#project-information

project = '{{ project }}'
copyright = '{{ author }}'
author = '{{ author }}'

# The full version, including alpha/beta/rc tags
release = '{{ version }}'

# -- General configuration ---------------------------------------------------
# https://www.sphinx-doc.org/en/master/usage/configuration.html#general-configuration

extensions = [
    'sphinx.ext.autodoc',  # Automatically documents modules, classes, and functions
    'sphinx.ext.napoleon',  # Supports Google style and NumPy style docstrings
    'sphinx.ext.viewcode',  # Adds links to the source code of documented Python objects
    'sphinx.ext.todo'       # Allows for the inclusion of TODO notes in the documentation
]

templates_path = ['_templates']
exclude_patterns = ['_build', 'Thumbs.db', '.DS_Store']

language = 'en'

# -- Options for HTML output -------------------------------------------------
# https://www.sphinx-doc.org/en/master/usage/configuration.html#options-for-html-output

html_theme = 'sphinx_rtd_theme' # change design
html_static_path = ['_static']

# -- Options for todo extension ----------------------------------------------
# https://www.sphinx-doc.org/en/master/usage/extensions/todo.html#configuration

todo_include_todos = True
